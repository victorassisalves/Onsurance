service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Only authenticated user can read and write data
      allow read, write: if request.auth != null;
    }
  }
}
